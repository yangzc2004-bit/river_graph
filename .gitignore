# Data (downloaded from USGS/WQP, reproducible via scripts/)
data/raw/
data/processed/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/

# pynhd download cache (NHDPlus VAA parquet, ~500MB, re-downloadable)
cache/

# presentation deck workspace (local only)
deck_build/
qa_ppt/
DESIGN.md
scripts/make_report_ppt.py
*.pptx

# local PDF work workspace
_pdf_work/

# A0 recovery cache: re-derivable from commit 256d08e via
# scripts/recover_historical_predictions.py; kept out of the repo so the
# canonical prediction batch stays unambiguous.
experiments/predictions_historical/
